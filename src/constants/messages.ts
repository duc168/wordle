const APPLICATION_IS_PROCESSING = 'Is processing...';

const EXCEED_MAX_ATTEMPT = 'Exceed maximum attempt.';

const EXCEED_MAX_CHARACTER = 'Exceed maximum character.';

const REACH_MIN_CHARACTER = 'No more character to remove.';

const ALREADY_DONE = 'Already done!';

export default {
  APPLICATION_IS_PROCESSING,
  EXCEED_MAX_ATTEMPT,
  EXCEED_MAX_CHARACTER,
  REACH_MIN_CHARACTER,
  ALREADY_DONE,
};
